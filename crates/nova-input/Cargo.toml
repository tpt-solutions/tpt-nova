[package]
name = "nova-input"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Keyboard/mouse input and action mapping for TPT Nova, wired into the ECS"

[dependencies]
winit = "0.30"
